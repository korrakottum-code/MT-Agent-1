# กฎของโปรเจกต์ MT Agent 1 สำหรับผู้ช่วย AI ทุกตัว

ไฟล์นี้คือมาตรฐานกลางที่ Codex, Cursor, Copilot, Gemini CLI, Windsurf และ Claude Code อ่านเองตอนเปิดโปรเจกต์
ถ้าเครื่องมือที่ใช้ไม่อ่านเอง ให้สั่งมันว่า "อ่าน AGENTS.md ก่อนเริ่มงาน"

## เริ่มจากตรงนี้

อ่าน `docs/HANDOFF.md` หัวข้อ "ทำต่อจากตรงนี้" ก่อนแตะโค้ด ในนั้นมีสถานะล่าสุด ผลข้อสอบ
และงานที่รอเจ้าของตัดสินใจ อย่าทำงานที่อยู่ในรายการ "ตัดทิ้งแล้ว" ซ้ำ

## โปรเจกต์นี้คืออะไร

บอทชื่อ "แงว" อยู่ในกลุ่มไลน์ของเอเจนซีการตลาด ช่วยทีมจดงาน ตั้งเตือน ค้นแชท และต่อกับ Monday.com
เจ้าของคือตั้ม (ADMIN) คุยกับเจ้าของและเขียนข้อความที่ผู้ใช้เห็นเป็นภาษาไทย

ไม่มีเซิร์ฟเวอร์ของตัวเอง ทุกอย่างอยู่บน Supabase โปรเจกต์ `ssjsjvcbulclnvlrkdsj` ศูนย์ข้อมูลสิงคโปร์

| ส่วน | ไฟล์ |
|---|---|
| ตัวรับไลน์และตอบแชท | `supabase/functions/line-webhook/index.ts` |
| งานเบื้องหลังตามเวลา | `supabase/functions/scheduled-jobs/index.ts` |
| ประตูเดียวที่คุยกับโมเดลทุกค่าย | `supabase/functions/_shared/models.ts` |
| หน้าแอดมิน | `supabase/functions/admin-console/index.ts` |
| โครงฐานข้อมูล | `supabase/migrations/` |
| ข้อสอบ | `tests/` |

สมองที่ใช้จริงคือ Gemini 3.7 Flash ทั้งตอบแชทและงานเบื้องหลัง ตัวสำรองคือ GPT-5.6 Luna
เครดิต Claude ของ Anthropic หมดแล้ว อย่าตั้งให้ใช้ Claude โดยไม่ถามเจ้าของ

## คำสั่งที่ใช้บ่อย

ตรวจโค้ดก่อน deploy ทุกครั้ง

```bash
npx deno@2 check supabase/functions/line-webhook/index.ts
```

deploy ขึ้นของจริง

```bash
npx supabase functions deploy line-webhook
```

รันข้อสอบ ต้องใส่ `-f model=gemini-flash` เสมอ ไม่งั้นไปใช้ sonnet ซึ่งเครดิตหมดแล้ว

```bash
gh workflow run agent-eval.yml -f model=gemini-flash -f cases=tests/cases-hard.json
```

ข้อสอบส่งรูปได้ด้วย ใส่ `image_url` หรือ `image_base64` ในคำขอ ใช้วัดว่าหารหัสงานจากรูปแล้วชัวร์จริงไหม

ทดสอบว่าส่งหลายบอลลูนแล้วตอบครั้งเดียว รันทุกครั้งที่แตะการรับข้อความ

```bash
python3 tests/burst-test.py "$CRON_SECRET"
```

`CRON_SECRET` เอามาจาก Supabase หน้า Edge Functions แล้ว Secrets ห้ามเขียนลงไฟล์

## กฎที่ห้ามพลาด

แต่ละข้อมาจากความเสียหายที่เกิดขึ้นจริงมาแล้ว

1. **ข้อสอบยิงเข้าระบบจริง** ใช้คนจริงเป็นผู้เรียกและเรียกเครื่องมือจริง
   เครื่องมือใหม่ที่เขียนข้อมูล ส่งข้อความ หรือแก้ Monday ต้องใส่ชื่อใน `WRITE_TOOLS`
   หรือเช็ค `ctx.dryRun` ก่อนลงมือ เคยมีข้อสอบส่งข้อความหาเจ้าของจริง และทิ้งงานปลอมไว้ 153 ใบ
2. **ตัดสินข้อสอบจากเครื่องมือที่ถูกเรียก ไม่ใช่ถ้อยคำ** regex ภาษาไทยเคยตัดสินคำตอบที่ถูกว่าผิดมาแล้วห้าครั้ง ครั้งล่าสุดคือแงวขอโทษแล้วยกคำเก่าของตัวเองมาแก้ แล้วข้อสอบไปจับคำเก่านั้น
   ถ้าต้องจับคำอ้างว่าทำแล้ว ใช้ `claim_needs_tool` ใน `tests/assert-case.ps1`
3. **ยืนยันกับของจริงก่อนบอกว่าแก้แล้ว** เรื่องตอบซ้ำหลายบอลลูนเคยรายงานว่าแก้แล้วสามรอบจากการอ่านโค้ด
   และผิดทั้งสามรอบ ถ้ายืนยันไม่ได้ให้บอกตรง ๆ ว่ายังไม่ได้ยืนยัน
4. **Monday อย่าฝังค่า** บอร์ดแต่ละตระกูลเก็บรหัสงานคนละคอลัมน์ ลำดับ `used_at` ไม่ตรงกับที่ทีมใช้
   และบอร์ดหนึ่งมีคอลัมน์สถานะได้ห้าอัน ถามบอร์ดก่อนทุกครั้ง บัญชีชื่อ `classclinic` มีราว 68 บอร์ด
5. **งานเกิดได้ทางเดียว คือมีคนสั่งและมีคนรับ** ถามแล้วตอบ คุยปรึกษา แจ้งข่าว รายงานว่าเสร็จ เป็นโน้ตทั้งหมด
   ในกลุ่มแงวอ่านและเก็บ ทำเฉพาะที่ถูกสั่ง
6. **ห้าม commit ความลับ** โทเคน กุญแจ รหัสผ่าน อยู่ใน Supabase Secrets เท่านั้น
7. **แก้ฐานข้อมูลต้องมีไฟล์ใน `supabase/migrations/` เสมอ** ให้รีโปกับฐานข้อมูลตรงกัน
8. **เครื่องที่รันแงวให้เวลาประมวลผลแค่ 2 วินาทีต่อข้อความ** รอเน็ตหรือรอฐานข้อมูลไม่นับ
   แต่งานที่ใช้เครื่องคิดล้วน ๆ อย่างย่อรูปทีละหลายใบจะโดนตัดกลางทาง
   รูปตัวอย่างที่ส่งเข้าไลน์จึงให้ที่เก็บไฟล์ของ Supabase ย่อให้ผ่าน `transform` ตอนทำลิงก์ ไม่ย่อเอง
9. **แงวต้องพูดความจริงเรื่องความเป็นส่วนตัว** ข้อความทุกอันถูกบันทึกและแอดมินดูได้
   เคยตอบพนักงานว่าไม่มีใครอ่านแชทส่วนตัวได้แล้วชวนนินทา กฎข้อ 16 ใน `SYSTEM_RULES` กันไว้แล้ว ห้ามลบ

## วิธีทำงานที่เจ้าของต้องการ

- ทำเสร็จแล้ว commit และ push ขึ้น main ได้เลย ไม่ต้องถาม
- ข้อความ commit เป็นภาษาอังกฤษ ขึ้นต้นด้วยคำกริยา อธิบายว่าทำไม ไม่ใช่แค่ทำอะไร
  ถ้าแก้แค่เอกสารให้ต่อท้ายหัวข้อด้วย `[skip ci]` จะได้ไม่เปลืองรอบข้อสอบ
- ทำเท่าที่สั่ง ไม่ต้องขยายงานเอง ถ้าเห็นปัญหาอื่นให้บอก แต่อย่าแก้โดยไม่ถาม
- คำตอบถึงเจ้าของให้สั้น ภาษาไทย บอกผลก่อน แล้วค่อยบอกเหตุผล
